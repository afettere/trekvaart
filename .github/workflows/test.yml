name: test
on:
  push:
    branches: [main]
  pull_request:
jobs:
  sluis:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: node --test sluis/spend-ceiling.test.mjs
      - run: bash -n install/bootstrap.sh install/firewall.sh runners/machinist/bin/sluis-run
